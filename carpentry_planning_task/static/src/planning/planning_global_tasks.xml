<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <div t-name="carpentry_planning_task.PlanningGlobalTasks" owl="1">
        <!-- <h2 class="mb-0 p-3 bg-white cursor-pointer" t-on-click="() => {this.state.visible = !this.state.visible}"> -->
        <!-- <i t-attf-class="fa {{ state.visible ? 'fa-chevron-up' : 'fa-chevron-down'}}" /> -->
        <h2 class="p-4 pb-2 mb-0 bg-white">
            Global Tasks
        </h2>
        <div t-att-class="{'d-none': !state.visible}">
            <View t-props="globalTasksViewProps" />
        </div>
    </div>

</templates>
